{% extends 'base.html.twig' %}

{% block title %}Elevators{% endblock %}

{% block body %}
    <h1>Elevators</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ elevator.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ elevator.name }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ elevator.status }}</td>
            </tr>
            <tr>
                <th>Position</th>
                <td>{{ elevator.position }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('elevators_index') }}">back to list</a>

    <a href="{{ path('elevators_edit', {'id': elevator.id}) }}">edit</a>

    {{ include('elevators/_delete_form.html.twig') }}
{% endblock %}
